version: '3.8'

services:
  cloudsync-api:
    build:
      context: .
      dockerfile: CloudSync/Dockerfile
    ports:
      - "5000:8080"
      - "5001: 8081"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - 